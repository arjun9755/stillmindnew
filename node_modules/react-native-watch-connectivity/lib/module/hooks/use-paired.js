"use strict";

import { useEffect, useState } from 'react';
import watchEvents from "../events/index.js";
import { getIsPaired } from "../paired.js";
export function usePaired() {
  const [paired, setPaired] = useState(false);
  useEffect(() => {
    getIsPaired().then(setPaired);
    return watchEvents.addListener('paired', setPaired);
  }, []);
  return paired;
}
//# sourceMappingURL=use-paired.js.map