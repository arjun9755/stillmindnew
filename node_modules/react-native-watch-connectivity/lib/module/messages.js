"use strict";

import { NativeModule } from "./native-module.js";
export function sendMessage(message, replyCb, errCb) {
  NativeModule.sendMessage(message).then(reply => {
    if (replyCb) {
      replyCb(reply);
    }
  }).catch(err => {
    if (errCb) {
      errCb(err);
    } else {
      console.warn('Unhandled sendMessage error', err);
    }
  });
}
//# sourceMappingURL=messages.js.map