"use strict";

import { getApplicationContext } from "../application-context.js";
import { useEffect, useState } from 'react';
import watchEvents from "../events/index.js";
export function useApplicationContext() {
  const [applicationContext, setApplicationContext] = useState(null);
  useEffect(() => {
    getApplicationContext().then(setApplicationContext);
    return watchEvents.addListener('application-context', setApplicationContext);
  }, []);
  return applicationContext;
}
//# sourceMappingURL=use-application-context.js.map