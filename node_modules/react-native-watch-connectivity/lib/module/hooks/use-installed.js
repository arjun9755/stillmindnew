"use strict";

import { useEffect, useState } from 'react';
import watchEvents from "../events/index.js";
import { getIsWatchAppInstalled } from "../installed.js";
export function useInstalled() {
  const [installed, setInstalled] = useState(false);
  useEffect(() => {
    getIsWatchAppInstalled().then(setInstalled);
    return watchEvents.addListener('installed', setInstalled);
  }, []);
  return installed;
}
//# sourceMappingURL=use-installed.js.map